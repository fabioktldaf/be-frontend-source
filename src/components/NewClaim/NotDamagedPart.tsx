import React from "react";
import styled from "styled-components";
import { Form, Input, Button, DatePicker } from "antd";
import { Row, RowSpacer } from "../../style/containers";
import { FormSubTitle } from "../../style/form";

const NotDamagedPartStyled = styled.div``;

const NotDamagedPart = () => {
  return (
    <NotDamagedPartStyled>
      <FormSubTitle>Parita Non Danno</FormSubTitle>
    </NotDamagedPartStyled>
  );
};

export default NotDamagedPart;
